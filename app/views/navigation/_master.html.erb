<% if current_user%>

  <div class="row">
    <div class="col-xl-8 offset-xl-2">

      <% if @user.spec.nil? && @user.foodexp.nil? && @user.housingexp.nil? && @user.tax.nil? && @user.foodexp.nil? && @user.miscexp.nil? && @user.utility.nil? %>
        <%= link_to "Master Overview", root_path, class: 'navbar-brand btn btn-outline-primary btn-block', id: 'master' %>
      <% else %>
        <%= link_to "Expense Overview", root_path, class: 'navbar-brand btn btn-outline-primary btn-block', id: 'master' %>
      <% end %>

    </div>
  </div>

<% end %>
